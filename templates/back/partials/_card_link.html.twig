		<div class="card" style="width: 18rem;">
			<div class="card-body">
				<h5 class="card-title">{{ entity|capitalize }}</h5>
				<a href="{{ path('app_back_' ~ entity ~ '_new')}}" class="card-link btn btn-success bi bi plus">Ajout</a>
				<a href="{{ path('app_back_' ~ entity ~ '_index')}}" class="card-link btn btn-primary bi bi list">Liste</a>
			</div>
		</div>

