{% extends 'base.html.twig' %}

{% block title %}Edition Plat{% endblock %}

{% block body %}
    <div>
    <h1>Edition Plat</h1>
        
    {{ include('back/eat/_form.html.twig', {'button_label': 'Sauvegarder'}) }}

     <a class="btn btn-warning" href="{{ path('app_back_eat_index') }}">{{ 'Retour' }}</a>

    {{ include('back/eat/_delete_form.html.twig') }}
    
</div>
{% endblock %}
